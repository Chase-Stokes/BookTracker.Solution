@{
  Layout = "_Layout";
}

@using Library.Models
@model List<Patron>
@* @model Library.Models.Patron *@

<h4>List of Patrons</h4>
@if (Model.Count > 0)
{
  @foreach (Patron join  in Model)
  {
    <li>@Html.ActionLink($"{join.Name}", "Details", new {id = join.PatronId})</li>
  }
}